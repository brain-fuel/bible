data/cache/
data/tokens.sqlite
__pycache__/
*.pyc
.pytest_cache/
.worktrees/

# SDD scratch (briefs, reports, review packages, ledger)
.superpowers/

# Derived MT<->LXX bridge (rebuildable projection)
align/mt-lxx/
data/cache/mt-lxx-bridge.jsonl
/data/cache

# Derived relation-graph files (rebuildable; run `python -m tools.build_relations`)
# relations/authored/ is NOT ignored — hand-curated CC0 truth, stays committed.
relations/derived/*.jsonl
